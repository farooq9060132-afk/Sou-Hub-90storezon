kind = "web"
previewPath = "/"
title = "90StorZon"
version = "1.0.0"
id = "artifacts/90storzon"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18980

[services.development]
run = "pnpm --filter @workspace/90storzon run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/90storzon", "run", "build" ]
publicDir = "artifacts/90storzon/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18980"
BASE_PATH = "/"
